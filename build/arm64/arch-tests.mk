# Supported tests by arm64

TESTS := $(ROOT)/tests/example
TESTS += $(ROOT)/tests/hyp-xen-version
TESTS += $(ROOT)/tests/arm-hypercall-fuzzer
TESTS += $(ROOT)/tests/arm-vgic-fuzzer
TESTS += $(ROOT)/tests/arm-structured-fuzzer
